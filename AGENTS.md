# AGENTS.md — pdns-mcp-server

This repo is a new, empty project intended to implement an MCP server for PowerDNS.

No code, config, or tooling has been set up yet. Before making significant additions, check for:
- a `README*` or root manifest (`package.json`, `Cargo.toml`, `pyproject.toml`, etc.)
- build/test/lint configuration
- CI workflows (`.github/workflows/`)

All guidance here is provisional until the project takes shape.